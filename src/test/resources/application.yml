spring:
  datasource:
    url: jdbc:h2:mem:operaton-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

operaton.bpm:
  admin-user:
    id: demo
    password: demo
  authorization:
    enabled: true
  database:
    schema-update: true
